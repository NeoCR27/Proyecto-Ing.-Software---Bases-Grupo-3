<script>
    function ValidateEndDate() {
        var startDate = $("fi").val();
        var endDate = $("fe").val();
        if (startDate != '' && endDate != '') {
            if (Date.parse(startDate) > Date.parse(endDate)) {
                $("txttodate").val('');
                alert("Start date should not be greater than end date");
            }
        }
        return false;
    }

</script>
